<form class="form-signin" action="/addplayer/{{league.id}}/{{player.id}}" method="post">
    <div>
        <div align="center">
            <label style="font-size: 20px;">Add Player "{{player.name}}" to your team?</label>
        </div>

        <div class="button" align="center">
            <button type="submit" class="btn btn-primary">Confirm</button>
        </div>
    </div>
</form>